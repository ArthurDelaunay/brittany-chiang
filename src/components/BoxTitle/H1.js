import styled from "styled-components"
import { lightGreen } from "../../styles/fontColors"
import { smallTitle } from "../../styles/fontSizes"
import { fontMono } from "../../styles/fontFamilies"

const H1 = styled.h1`
  color: ${lightGreen};
  font-size: ${smallTitle};
  font-family: ${fontMono};
`
export default H1
